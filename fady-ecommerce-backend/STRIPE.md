# Stripe setup

To enable Stripe payments, add the following variables to fady-ecommerce-backend/.env:

STRIPE_KEY=pk_test_xxx
STRIPE_SECRET=sk_test_xxx

Set APP_CURRENCY if you want a currency other than USD (default: usd).
